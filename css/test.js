var is=[1,2,3,4,5,6];
var sum=[];
var number;
for (var a=0;a<=is.length-1;a++){
  for (var b=is.length-1;b>=a;b--){
    number=0;
    // number = number + is[b];
    console.log(is[b])
  }
  sum[a]=number;
  // console.log(sum);
}